<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit the details</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>You are about to change the details for @<%= result.username %></h1>
        <form action="/users/<%= result.id %>?_method=PATCH" method="post">
            <div class="form-group">
                <textarea name="username" id="username" class="form-control" rows="1" required><%= result.username %></textarea>
            </div>
            <div class="form-group">
                <input type="password" name="password" id="password" class="form-control" placeholder="Password" required>
            </div>
            <button class="btn btn-primary btn-block">Submit</button>
        </form>
    </div>
</body>
</html>
